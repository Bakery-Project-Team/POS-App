export interface Inventory{
  
    itemNo: number;
    qty: number;
    price: number;
    frequency: number;
    storedPrice: number; // can be adjusted for VAT later
    packs: number;
    numPerPack: number;  
    
}

